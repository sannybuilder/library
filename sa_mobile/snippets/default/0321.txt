float x, y, z, radius = 15.0

while true
    wait 2000
    x, y, z = get_active_camera_coordinates
    if
        Char charHandle = get_random_char_in_sphere_no_save_recursive x y z radius findNext True filter CharSearchFilter.Any
    then
        explode_char_head charHandle
    end
end
