if SAMP_KEY_JUST_PRESSED {keyCode} KeyCode.R
then // user initially pressed the 'R' key
else
    // either user isn't pressing the 'R' key
    // or user is still pressing the 'R' key and hasn't released it yet
end