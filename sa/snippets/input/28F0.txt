while
    wait {time} 0 // any other than 0 will cause is_key_just_pressed to sleep through some key presses

    if
        is_key_just_pressed {keyCode} KeyCode.A
    then
        print_help_string {text} "Key pressed!"
    end
end