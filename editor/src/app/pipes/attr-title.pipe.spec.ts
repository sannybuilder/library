import { AttrTitlePipe } from './attr-title.pipe';

describe('AttrTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new AttrTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
