while true
    wait 0
    if
        locate_object_distance_to_object objectHandle {object} targetObject {radius} 5.0
    then
        // object is within the radius of the specified object
    end
end
