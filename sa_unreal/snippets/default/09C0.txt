const posOffset = 120.0, negOffset = -120.0, NumLock = 144
float x, y, z, x2, y2, z2

while true
    wait 0
    if
        is_key_just_pressed NumLock
    then
        x, y, z = get_offset_from_char_in_world_coords $scplayer posOffset posOffset posOffset
        x2, y2, z2 = get_offset_from_char_in_world_coords $scplayer negOffset negOffset negOffset
        Car carHandle = get_random_car_of_type_in_angled_area_no_save x2 y2 x y 20.0 #TAXI
        if
            carHandle <> -1
        then
            int blipHandle = add_blip_for_car carHandle
        end
    end
end
