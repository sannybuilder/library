float x, y, z, radius = 15.0

while true
    wait 2000
    x, y, z = get_char_coordinates $scplayer
    if
        Char charHandle = get_random_char_in_sphere_no_save_recursive x y z radius findNext True skipDead True
    then
        explode_char_head charHandle
    end
end
