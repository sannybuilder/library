float x, y, z, nodeX, nodeY, nodeZ, heading

while true
    wait 0
    int interiorId = get_area_visible
    if and
        is_key_pressed {keyCode} KeyCode.F10
        interiorId == 0
    then
        while is_key_pressed {keyCode} KeyCode.F10
            wait 0
        end
        request_model #TAXI
        load_all_models_now
        x, y, z = get_active_camera_coordinates
        nodeX, nodeY, nodeZ, heading = get_closest_car_node_with_heading x y z
        clear_area nodeX nodeY nodeZ 3.0 False
        Car carHandle = create_car #TAXI nodeX nodeY nodeZ
        set_car_heading carHandle heading
        mark_car_as_no_longer_needed carHandle
        mark_model_as_no_longer_needed #TAXI
    end
end
