float float fromCoord[3], toCoord[3],
// do something to fromCoord and toCoord

float colPoint[3]
int colPointPtr = get_var_pointer colPoint
int isWaterDetected = TestLineAgainstWater(...fromCoord, ...toCoord, colPointPtr)
if isWaterDetected
then print_formatted_now "Water Detected at~n~%f %f %f" 100 ...colPoint
end