set_current_directory {path} 0 // restore default as game will crash trying to load files (prior to CLEO5)
give_player_clothes_outside_shop $scplayer {textureName} "tash" {modelName} "cornrows" {bodyPart} BodyPart.Head
build_player_model $scplayer
set_player_model $scplayer {modelId} 0