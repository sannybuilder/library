const inputStr = "values = 4, 5"

int num1, num2, readCount
longString str // can also be buffer from allocate_memory

if
    readCount, str, num1, num2 = scan_string {string} inputStr {format} "%s = %d, %d"
then
    // success!
    // readCount is 3
    // str is "values"
    // num1 is 4
    /// num2 is 5
else
    // failed!
    // readCount will inform how many parameters was successfully parsed before error
end