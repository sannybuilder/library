request_model #WOODENBOX
load_all_models_now
Object objHandle = create_object {modelId} #WOODENBOX {pos} 2479.0 -1660.0 13.0
mark_model_as_no_longer_needed #WOODENBOX

while true
    wait 0
    if
        is_char_touching_object $scplayer {object} objHandle
    then
        print_string_now {text} "Touching the box!" {time} 100
    end
end
