float x, y, z, radius = 10.0

while true
    wait 100
    x, y, z = get_char_coordinates $scplayer
    if
        Car carHandle = get_random_car_in_sphere_no_save_recursive x y z radius findNext True skipWrecked True
    then
        lock_car_doors carHandle CarLock.LockoutPlayerOnly
    end
end
