while true
    wait 0
    if
        locate_char_in_car_char_2d $scplayer {otherChar} targetChar {xRadius} 2.0 {yRadius} 2.0 {drawSphere} true
    then
        // char is within the 2D radius of the other char in a vehicle
    end
end
